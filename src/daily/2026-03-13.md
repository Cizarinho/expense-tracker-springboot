# Tagesprotokoll – 2026-03-13

## 1. Ziel des Tages
Konzeption des Datenmodells und der Architektur (Design Patterns) abschließen, um bereit für die Programmierung zu sein.

## 2. Erledigt
- Klassenstruktur theoretisch entworfen (Interface `Transaction`, Klassen `Income` und `Expense`).
- Entscheidung für das Factory Pattern (`TransactionFactory`) getroffen, um Einnahmen und Ausgaben sauber zu trennen.
- Zeitplan für die kommenden Wochen finalisiert.
- Überlegungen zur Datenbankanbindung (Nutzung von Spring Data JPA) angestellt.

## 3. Probleme / offene Fragen
- Starten wir für die Entwicklung direkt mit einer "echten" Datenbank (MySQL) oder nutzen wir erstmal eine H2 In-Memory-Datenbank?

## 4. Nächster Schritt
Start der eigentlichen Programmierung: Die `pom.xml` für die Datenbank vorbereiten und die ersten Domain-Klassen (Code) anlegen.