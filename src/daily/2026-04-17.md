# Tagesprotokoll – 2026-04-17

## 1. Ziel des Tages

Wiederaufnahme des Projekts nach einer Pause. Implementierung der Datenzugriffs- und Logikschicht(Repository, Factory und Service)

## 2. Erledigt
- Erneuter Verbindungstest zur Datenbank erfolgreich durchgeführt
- `TransactionRepository` erstellt: Ermöglicht den Zugriff auf die Datenbank ohne manuelles SQL.
- `TransactionFactory` implementiert: Usetzung des **Factory Patterns** zur kontrollierten Erstellung von `Income` und `Expense`-Objekten inklusive **Guards**.
- `TransactionService` erstellt: Zentrale Geschäftslogik, die Repository und Factory verbindet.
- Methode zur Kontostandsberechnung implementiert, welche die **Polymorphie** der `getSignedAmount()`-Methode nutzt.

## 3. Probleme / offene Fragen
- Keine Probleme

## 4. Nächster Schritt
Erstellung der Controller-Schicht um die Logik über das Web-Frontend(Thymeleaf) zugänglich zu machen.
