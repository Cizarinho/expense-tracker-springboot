# Tagesprotokoll – 2026-02-26

## 1. Ziel des Tages

Die Datenbankanbindung konfigurieren und das Domain-Modell in Java umsetzen

## 2. Erledigt
- `pom.xml` um den MySQL-Treiber und Spring Data JPA erweitert.
- `application.properties` für die Verbindung zur lokalen Datenbank (`expense-tracker`) konfiguriert.
- Erfolgreicher Verbindungstest zur Datenbank durchgeführt.
- Das ER-Modell theoretisch mit der *Single Table Strategy* abgeglichen.
- Domain-Klassen im Paket `model` erstellt:
    - Interface `Transaction`
    - Abstrakte Basisklasse `AbstractTransaction` (mit JPA-Annotationen)
    - Abgeleitete Klassen `Income` und `Expense` (Polymorphie implementiert: `getSignedAmount()`).

## 3. Probleme / offene Fragen

- Keine Probleme. Die Datenbankverbindung hat auf Anhieb funktioniert und Spring Boot hat die Tabellenstruktur dank `@Entity` fehlerfrei generiert.

## 4. Nächster Schritt

Das `TransactionRepository` (Datenbankzugriff) anlegen und das *Factory Pattern* (`TransactionFactory`) implementieren
